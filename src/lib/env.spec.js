// Copyright (c) 2020 devmastery

const { setupEnv } = require("./env")

describe("ENV", () => {
  it("should respect both files", () => {
    // setupEnv()
    // expect(process.env.TITLE).toBe('onepile-signal')
    // expect(process.env.TIMOUT).toBe('30000')
  })
})
